apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: app-namespace
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  type: ClusterIP