apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-db
  namespace: app-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product_db
  template:
    metadata:
      labels:
        app: product_db
    spec:
      containers:
        - name: product-db
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "product-service"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "12345"

---
apiVersion: v1
kind: Service
metadata:
  name: product-db
  namespace: app-namespace
spec:
  selector:
    app: product-db
  ports:
    - port: 5432
      targetPort: 5432