apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-db
  namespace: app-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user_db
  template:
    metadata:
      labels:
        app: user_db
    spec:
      containers:
        - name: user-db
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "user-service"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "12345"

---
apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: app-namespace
spec:
  selector:
    app: user-db
  ports:
    - port: 5432
      targetPort: 5432